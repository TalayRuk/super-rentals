import FirebaseAdapter from 'emberfire/adapters/firebase';

export default FirebaseAdapter.extend({
});
//Adapter responsible for connecting applications to their data stores. 
//In this case meant to connecting to Firebase.
// this file is create when run $ ember instal emberfire
